<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Contrast" parent="AppTheme">
        <item name="android:windowBackground">@color/colorOrangeBackground</item>
        <item name="android:textColor">#26CB26</item>
        <item name="android:colorBackground">@color/colorOrangeBackground</item>
        <item name="backgroundColor">@color/colorOrangeBackground</item>
        <item name="android:backgroundTint">@color/colorOrangeBackground</item>
        <item name="android:background">@color/colorOrangeBackground</item>
    </style>

    <style name="Dark" parent="AppTheme">
        <item name="android:windowBackground">@color/ColorDarkBackground</item>
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:colorBackground">@color/ColorDarkBackground</item>
        <item name="backgroundColor">@color/ColorDarkBackground</item>
        <item name="android:backgroundTint">@color/ColorDarkBackground</item>
        <item name="android:background">@color/ColorDarkBackground</item>
    </style>

</resources>